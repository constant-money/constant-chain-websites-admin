@layout('template.admin-master')
@section('content_name')
User List
@endsection
@section('content')
    <form class="form-inline">
        <div class="form-group mx-sm-3 mb-2">
            <label for="email" class="sr-only">Email</label>
        <input type="text" class="form-control" name="email" id="email" placeholder="Email" value="{{ email }}">
        </div>
        <button type="submit" class="btn btn-primary mb-2">Find</button>
    </form>
    <br>
    <table class="table table-hover table-sm">
        <thead class="thead-light">
            <tr>
            <th scope="col">#</th>
            <th scope="col">Email</th>
            <th scope="col">First Name</th>
            <th scope="col">Last Name</th>
            <th scope="col">Date Created</th>
            </tr>
        </thead>
        <tbody>
            @each(user in users)
                <tr>
                <th scope="row"><a href="{{ route('Admin/UserController.detail', { id : user.id }) }}">{{ (page - 1) * perPage + $loop.index + 1}}</a></th>
                    <td class="text-truncate" style="max-width: 100px;">{{ user.email }}</td>
                    <td class="text-truncate" style="max-width: 100px;">{{ user.first_name }}</td>
                    <td class="text-truncate" style="max-width: 100px;">{{ user.last_name }}</td>
                    <td>{{moment(user.created_at).format("DD-MM-YYYY")}}</td>
                </tr>
            @endeach
        </tbody>
    </table>
    <br>
    @!component('admin.components.pagination', url_prefix=(route('Admin/UserController.index') + '?email=' + email + '&'), page=page, lastPage=lastPage, perPage=perPage)
@endsection
@section('suffix_script')
<script>
    $(document).ready(function(){
        $("#email").on("keyup", function() {
            $("#form" ).submit();
        });
    });
</script>
@endsection