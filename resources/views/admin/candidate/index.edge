@layout('template.admin-master')
@section('content_name')
Candidate List
@endsection
@section('content')
    <form class="form-inline">
        <div class="form-group mx-sm-3 mb-2">
            <label for="email" class="sr-only">Email</label>
            <input type="email" class="form-control" name="email" id="email" placeholder="Email" value="{{ email }}">
        </div>
        <div class="form-group mx-sm-3 mb-2">
            <label for="dcb" class="sr-only">DCB</label>
            <input type="text" class="form-control" name="dcb" id="dcb" placeholder="DCB" value="{{ dcb }}">
        </div>
        <div class="form-group mx-sm-3 mb-2">
            <label for="cmb" class="sr-only">CMB</label>
            <input type="text" class="form-control" name="cmb" id="cmb" placeholder="CMB" value="{{ cmb }}">
        </div>
        <div class="form-group mx-sm-3 mb-2">
            <label for="gov" class="sr-only">GOV</label>
            <input type="text" class="form-control" name="gov" id="gov" placeholder="GOV" value="{{ gov }}">
        </div>
        <button type="submit" class="btn btn-primary mb-2">Find</button>
    </form>
    <br>
    <table class="table table-hover table-sm">
        <thead class="thead-light">
            <tr>
            <th scope="col">#</th>
            <th scope="col">Email</th>
            <th scope="col">DCB</th>
            <th scope="col">CMB</th>
            <th scope="col">GOV</th>
            <th scope="col">Date Created</th>
            <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @each(votingBoardCandidate in votingBoardCandidates)
                <tr>
                <th scope="row"><a href="{{ route('admin.candidate.show', { id : votingBoardCandidate.id }) }}">{{ votingBoardCandidate.id }}</a></th>
                    <td class="text-truncate" style="max-width: 100px;">
                        <a href="{{ route('admin.user.show', { id : votingBoardCandidate['$relations'].user.id }) }}">{{ votingBoardCandidate['$relations'].user.email }}</a>
                    </td>
                    <td class="text-truncate" style="max-width: 150px;">{{ votingBoardCandidate.dcb }}</td>
                    <td class="text-truncate" style="max-width: 150px;">{{ votingBoardCandidate.cmb }}</td>
                    <td class="text-truncate" style="max-width: 150px;">{{ votingBoardCandidate.gov }}</td>
                    <td>{{moment(votingBoardCandidate.created_at).format("DD-MM-YYYY")}}</td>
                    <td class="text-truncate" style="max-width: 30px;">
                        <a href="{{ route('admin.candidate.voterindex', { id : votingBoardCandidate.id }) }}">Voters ({{votingBoardCandidate.vote_count}})</a>
                    </td>
                </tr>
            @endeach
        </tbody>
    </table>
    <br>
    @!component('admin.components.pagination', urlPrefix=(route('admin.candidate.index') + '?email=' + email + '&'), page=page, lastPage=lastPage, perPage=perPage)
@endsection
@section('suffix_script')
<script>
    $(document).ready(function(){
        $("#email").on("keyup", function() {
            $("#form" ).submit();
        });
    });
</script>
@endsection
